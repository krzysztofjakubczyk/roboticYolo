package com.sanbot.demo;import android.app.Application;import android.content.Context;import com.sanbot.demo.entity.Constant;import com.sanbot.demo.util.AppUtil;import com.sanbot.demo.util.CrashHandler;public class QHApplication extends Application {    private static final String TAG = "QHApplication";    public static QHApplication application = null;    public static Context getApplication() {        return application;    }    @Override    public void onCreate() {        super.onCreate();        application = this;        //debug 版本时，不需要处理异常        if (!AppUtil.isApkDebugAble(getApplicationContext())) {            CrashHandler.getInstance().init(this);            Constant.DEBUG = false;        }    }}