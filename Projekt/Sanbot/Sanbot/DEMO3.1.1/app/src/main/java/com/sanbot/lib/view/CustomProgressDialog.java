package com.sanbot.lib.view;import android.app.Dialog;import android.content.Context;import android.graphics.drawable.AnimationDrawable;import android.text.TextUtils;import android.view.LayoutInflater;import android.view.View;import android.widget.ImageView;import android.widget.LinearLayout;import android.widget.TextView;import com.sanbot.demo.R;/** * 自定义progress dialog */public class CustomProgressDialog {    public static Dialog create(Context context, String msg) {        LayoutInflater inflater = LayoutInflater.from(context);        View v = inflater.inflate(R.layout.dialog_progress, null);        TextView textView = (TextView) v.findViewById(R.id.progress_tv);        if (TextUtils.isEmpty(msg)) {            textView.setVisibility(View.GONE);        } else {            textView.setVisibility(View.VISIBLE);            textView.setText(msg);// 设置加载信息        }        /*ImageView loaddinView = (ImageView) v.findViewById(R.id.loadding);        if (loaddinView != null) {            AnimationDrawable drawable = (AnimationDrawable) loaddinView.getDrawable();            if (drawable != null) {                drawable.stop();                drawable.selectDrawable(0);                drawable.start();            }        }*/        Dialog dialog = new Dialog(context, R.style.loading_dialog);// 创建自定义样式dialog        dialog.setContentView(v, new LinearLayout.LayoutParams(                LinearLayout.LayoutParams.WRAP_CONTENT,                LinearLayout.LayoutParams.WRAP_CONTENT));// 设置布局        dialog.setCancelable(true);        dialog.setCanceledOnTouchOutside(false);        return dialog;    }}